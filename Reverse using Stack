

string reverse(const string& S) {
        // code here
        stack<char>st;
        for (char c : S) {
            st.push(c);
        }
        string ans="";
        while(!st.empty()) {
            ans+=st.top();
            st.pop();
        }
        return ans;
    }
